#include <stdio.h>
#include <stdlib.h>
#include "middle_code_gen.h"

// int main() {
//     char result[1024] = { 0 };
//     past node1 = (past) malloc(sizeof(ast)); node1->nodeType = BINARY_OPERATOR;

//     node1->left = (past) malloc(sizeof(ast)); node1->left->nodeType = INTEGER_LITERAL;
//     node1->left->ivalue = 5;
//     node1->right = (past) malloc(sizeof(ast)); node1->right->nodeType = INTEGER_LITERAL;
//     node1->right->ivalue = 10;
//     genArithmeticExpr(node1, result);
//     printf("%s\n", result);

//     // 清理内存
//     free(node1->left);
//     free(node1->right);
//     free(node1);

//     return 0;
// }

int main() {
    return 0;
}
